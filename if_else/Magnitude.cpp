#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"enter a number:";
    cin>>n;
    if(n > -69 && n < 69)
    cout<<"magnitude is smaller than 69";
    else
    cout<<"magnitude is  not smaller than 69";
}